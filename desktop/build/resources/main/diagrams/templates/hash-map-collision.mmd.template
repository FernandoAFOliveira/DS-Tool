%% @title Hash collision
%% @subtitle Different keys can select the same bucket
flowchart TB

    HashA["hash key: blue"]:::action -.-> Bucket
    HashB["hash key: teal"]:::action -.-> Bucket
    subgraph Storage["bucket storage"]
        direction LR
        Bucket["bucket 2"]:::focus --> A["blue maps to 42"]:::data
        A --> B["teal maps to 17"]:::data
    end
    Compare["compare keys inside bucket<br/>to find the matching entry"]:::note -.-> B
    class Storage container
