%% @title Hash set
%% @subtitle Unique values organized by hash bucket
flowchart TB

    subgraph Buckets["bucket storage"]
        direction LR
        B0["bucket 0"]:::data --> Apple["apple"]:::data
        B1["bucket 1"]:::focus --> Banana["banana"]:::focus
        B2["bucket 2"]:::data --> Kiwi["kiwi"]:::data
        B3["bucket 3<br/>empty"]:::note
    end
    Find["contains banana<br/>hash selects bucket 1"]:::action -.-> B1
    Duplicate["insert apple<br/>duplicate rejected"]:::warning -.-> Apple
    class Buckets container
