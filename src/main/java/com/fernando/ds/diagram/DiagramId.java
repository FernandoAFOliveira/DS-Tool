package com.fernando.ds.diagram;

/** Stable concept-owned identity for a primary or optional diagram view. */
public enum DiagramId {
    STRUCTURE,
    RESIZE_COPY,
    CIRCULAR_BUFFER,
    ARRAY_LAYOUT,
    COLLISION_HANDLING,
    BALANCING
}
